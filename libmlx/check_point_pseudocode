
check(A, B, XMAX, YMAX)




redefinir points
dy = y1 - y0
dx = x1 - x0
new_p0 = p0
new_p1 = p1

gradient = dy / dx

si les deux points sont dans le plan ==> ok

sinon
{
	(si (p0 != PLAN) && (new_p0 = find_proj(p0, p1, axe) == p0))
		=> pas secante RETURN
	(si (p1 != PLAN) && (new_p1 = find_proj(p1, new_p0, axe) == p1))
		=> pas secante RETURN
}
